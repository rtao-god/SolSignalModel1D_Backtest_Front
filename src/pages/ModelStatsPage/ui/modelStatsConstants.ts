import type { SegmentKey } from './modelStatsTypes'

/*
	modelStatsConstants — префиксы сегментов и порядок их выбора для отчёта статистики моделей.
	Используются в фильтрации table-секций, определении доступных сегментов и выборе стартового сегмента.

	Зачем:
		- Держит единые маркеры заголовков секций.
		- Фиксирует стабильный порядок сегментов для UX.

	Контракты:
		- Префиксы должны строго совпадать с форматом заголовков в отчёте.
*/

/*
	Префиксы сегментов в title секций.

	- Нужны для детектора доступных сегментов и фильтрации таблиц.
*/
export const SEGMENT_PREFIX: Record<SegmentKey, string> = {
    FULL: '[FULL] ',
    TRAIN: '[TRAIN] ',
    OOS: '[OOS] ',
    RECENT: '[RECENT] '
}

/*
	Порядок сегментов для авто-выбора стартового.

	- Держит стабильный и предсказуемый UX.
*/
export const SEGMENT_INIT_ORDER: SegmentKey[] = ['OOS', 'RECENT', 'TRAIN', 'FULL']
