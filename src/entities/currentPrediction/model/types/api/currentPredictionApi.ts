/* export const currentPredictionApi = baseApi.injectEndpoints({
    endpoints: builder => ({
        getCurrentPrediction: builder.query<CurrentPrediction, void>({
            query: () => ({
                url: '/current-prediction',
                method: 'GET'
            })
        })
    })
})

export const { useGetCurrentPredictionQuery } = currentPredictionApi
 */